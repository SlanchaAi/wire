Will — RFC-006 up for your review: docs/rfc/0006-confidentiality-roadmap-sequencing.md

Context: I went down a "wire reinvented DIDComm/MLS/NIP-44, big architecture miss" rabbit hole — then read THREAT_MODEL + BACKLOG + ANTI_FEATURES and it all collapsed. Those are deliberate, documented deferrals with crates already picked (NIP-44 over authcrypt, vodozemac for FS/PCS, OpenMLS for groups). No scandal. RFC claims zero discoveries.

The ONE real new thing it forces: confidentiality is deferred to v0.2/v0.3, but RFC-005 just made v0.15 a break-freely / no-prod-users window. If NIP-44 body-encryption changes the event wire-format, deferring past v0.15 = a SECOND format break for the same users.

Recommendation: reserve `enc` body field + `dh_pubkey` card field in v0.15 (plaintext-default, ~3 paragraphs, no code), so NIP-44 lands ADDITIVELY in v0.2 instead of risking a second break.

Two decisions I want your read on:
1. Is NIP-44 path-A (additive, reserve & stay schema v3) or path-B (breaking, must ride v0.15)? You know the canonicalization + pull-side better than me.
2. FS/PCS route: vodozemac-from-pairing-secret (no card field, reuses the SPAKE2 secret we already derive then throw away) vs X25519-on-card NIP-44 (gets us Nostr-DM interop). Or both. Your call zone.

Plus one genuine doc gap the code pass found: group FS/PCS/eviction deferral is NOT written in THREAT_MODEL the way DM-plaintext T1 is. Should add a parity entry.

Operating principle paul just ratified: reuse > build — pull crates (NIP-44/vodozemac/OpenMLS), don't author crypto. RFC follows that.

What breaks in your mental model? Especially want push-back on the path-A-vs-B call before we freeze the v0.15 schema.
